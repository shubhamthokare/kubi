apiVersion: external-secrets.io/v1beta1
kind: ClusterSecretStore
metadata:
  name: gcp-secret-store
spec:
  provider:
    gcpsm:
      projectID: kubi-gcp-project
      auth:
        secretRef:
          secretAccessKeySecretRef:
            name: gcpsm-service-account-key
            key: key.json
            namespace: kube-system
