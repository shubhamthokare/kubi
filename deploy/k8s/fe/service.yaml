apiVersion: v1
kind: Service
metadata:
  name: kubi-frontend-service
  namespace: kubi
spec:
  type: ClusterIP
  selector:
    app: kubi-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
