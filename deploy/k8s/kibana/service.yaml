apiVersion: v1
kind: Service
metadata:
  name: kibana-service
  namespace: kubi
  labels:
    app: kibana
spec:
  selector:
    app: kibana
  ports:
    - name: ui
      port: 5601
      targetPort: 5601
      protocol: TCP
  type: ClusterIP
