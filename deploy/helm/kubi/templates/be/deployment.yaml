apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "kubi.fullname" . }}-backend
  namespace: {{ .Values.namespace | default .Release.Namespace }}
  labels:
    {{- include "kubi.labels" . | nindent 4 }}
    component: backend
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "kubi.selectorLabels" . | nindent 6 }}
      component: backend
  template:
    metadata:
      labels:
        {{- include "kubi.selectorLabels" . | nindent 8 }}
        component: backend
    spec:
      serviceAccountName: {{ include "kubi.fullname" . }}-backend-sa
      containers:
      - name: backend
        image: "{{ .Values.backend.image.repository }}:{{ .Values.backend.image.tag | default .Chart.AppVersion }}"
        imagePullPolicy: {{ .Values.backend.image.pullPolicy }}
        env:
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: {{ include "kubi.fullname" . }}-secrets
              key: DB_PASSWORD
        - name: JWT_SECRET_KEY
          valueFrom:
            secretKeyRef:
              name: {{ include "kubi.fullname" . }}-secrets
              key: JWT_SECRET_KEY
        - name: RESEND_API_KEY
          valueFrom:
            secretKeyRef:
              name: {{ include "kubi.fullname" . }}-secrets
              key: RESEND_API_KEY
        - name: GEMINI_API_KEY
          valueFrom:
            secretKeyRef:
              name: {{ include "kubi.fullname" . }}-secrets
              key: GEMINI_API_KEY
        - name: GITLAB_TOKEN
          valueFrom:
            secretKeyRef:
              name: {{ include "kubi.fullname" . }}-secrets
              key: GITLAB_TOKEN
        - name: MONGODB_URL
          valueFrom:
            configMapKeyRef:
              name: {{ include "kubi.fullname" . }}-config
              key: MONGODB_URL
        - name: DATABASE_NAME
          valueFrom:
            configMapKeyRef:
              name: {{ include "kubi.fullname" . }}-config
              key: DATABASE_NAME
        ports:
        - containerPort: {{ .Values.backend.service.port }}
        resources:
          {{- toYaml .Values.resources | nindent 10 }}
